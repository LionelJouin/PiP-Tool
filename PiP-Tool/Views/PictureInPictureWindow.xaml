<Window x:Class="PiP_Tool.Views.PictureInPictureWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="PictureInPictureWindow" 
        ResizeMode="CanResizeWithGrip" 
        AllowsTransparency="True"
        Height="{Binding WindowSize.Height, UpdateSourceTrigger=PropertyChanged}" 
        Width="{Binding WindowSize.Width, UpdateSourceTrigger=PropertyChanged}" 
        WindowStyle="None" 
        Topmost="True"
        WindowStartupLocation="Manual">
    <Window.Background>
        <SolidColorBrush Opacity="0.0" Color="Black"/>
    </Window.Background>
    <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch">

        <Image Source="{Binding ImageScreen, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,0"/>

    </Grid>
</Window>
